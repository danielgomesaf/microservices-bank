# Start with a base image containing Java runtime
FROM openjdk:11-slim as build

# Information around who maintains the image
LABEL maintainer="daniel.gomes"

# Add the application's jar to the container
COPY target/accounts-0.0.1-SNAPSHOT.jar accounts-0.0.1-SNAPSHOT.jar

# Execute the application
ENTRYPOINT ["java", "-jar", "/accounts-0.0.1-SNAPSHOT.jar"]


# Docker command to create image from this docker file
# docker build . -t TAG_NAME
# . refers to the directory where the Dockerfile is located inside the project
# -t indicates a tag name to replace the random number generated by docker to identify a container